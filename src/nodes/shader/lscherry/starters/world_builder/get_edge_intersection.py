# ============================================================
# AUTO-GENERATED by LSPotato NodeCompiler
# Do NOT edit manually — regenerate from the source .blend file.
# ============================================================

import bpy  # type: ignore
from mathutils import Color, Euler, Matrix, Quaternion, Vector  # type: ignore
from .....node import ShaderNode


class ShaderNodeCompiled_Get_Edge_Intersection(ShaderNode):
    bl_idname = 'ShaderNodeCompiled_Get_Edge_Intersection'
    bl_label = 'lscherry.starters.world_builder.Get Edge Intersection'
    bl_icon = "NONE"
    _PREFIX = "."

    def draw_label(self):
        return 'Get Edge Intersection'

    def init(self, context):
        self.getNodetree(self.name + '_node_tree')

    def createNodetree(self, name):
        # Use bl_label as a stable, class-level key so all instances share
        # one node tree and nested references resolve correctly.
        nt = self.node_tree = bpy.data.node_groups.new(
            self._PREFIX + self.bl_label, 'ShaderNodeTree'
        )
        nt.color_tag = 'INPUT'

        _sock_out_Fac = nt.interface.new_socket(name='Fac', in_out='OUTPUT', socket_type='NodeSocketFloat')
        _sock_out_Fac.default_value = 0.0
        _sock_out_Fac.min_value = -3.4028234663852886e+38
        _sock_out_Fac.max_value = 3.4028234663852886e+38

        Group_Output = nt.nodes.new('NodeGroupOutput')
        Group_Output.location = (190.0, 0.0)
        Group_Output.is_active_output = True

        Group_Input = nt.nodes.new('NodeGroupInput')
        Group_Input.location = (-200.0, 0.0)

        Attribute = nt.nodes.new('ShaderNodeAttribute')
        Attribute.location = (0.0, 0.0)
        Attribute.attribute_type = 'GEOMETRY'
        Attribute.attribute_name = 'egIns'


        nt.links.new(Attribute.outputs['Factor'], Group_Output.inputs['Fac'])
